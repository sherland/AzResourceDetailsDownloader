# afwd5edfm

| Property | Value |
|---|---|
| **Resource group** | rg-ardl-286cff30700da727 |
| **Location** | Norway East |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Virtual network** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "vnetw4qxan-j" --> *Not available from captured ARM metadata.* |
| **Provisioning state** | *See the Azure Portal for current status.* |
| **SKU** | <!-- TODO (ShortcutMismatch): Computed "Basic (AZFW_VNet)" does NOT match portal "Basic(change)" for this type — do not trust this shortcut here. Captured example: "Basic(change)" --> *Not available from captured ARM metadata.* |
| **Subnet** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "AzureFirewallSubnet" --> *Not available from captured ARM metadata.* |
| **Public IP** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "piph5-e48-s" --> *Not available from captured ARM metadata.* |
| **Private IP** | 10.45.255.4 |
| **NAT gateway** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "-" --> *Not available from captured ARM metadata.* |
| **Management subnet** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "AzureFirewallManagementSubnet" --> *Not available from captured ARM metadata.* |
| **Management public IP** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "pipunesj-jt" --> *Not available from captured ARM metadata.* |
| **Private IP Ranges** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "IANA RFC 1918" --> *Not available from captured ARM metadata.* |
| **Route Server** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "Add" --> *Not available from captured ARM metadata.* |
