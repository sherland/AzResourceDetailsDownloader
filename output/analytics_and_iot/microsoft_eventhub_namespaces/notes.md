# Portal notices

Text automatically extracted from info/warning boxes on the Overview blade at capture time (deprecation notices, security recommendations, breaking-change warnings, etc.) for `Microsoft.EventHub/namespaces`.

- You can start generating test data or inspect data that has already been sent with the new Azure Event Hubs Data Explorer. Click on this message to try the feature!

- The namespace is currently in a failed state due to the following error: ''. To resolve this issue, address the root cause and retry the operation. For more details, refer to the Activity Logs.
                Retry

- Your current minimum TLS version setting is below TLS 1.2, which may indicate that you are using TLS 1.0 or TLS 1.1 to connect to this namespace. With Event Hubs deprecating TLS 1.0 and TLS 1.1 on February 28, 2025, we highly recommend migrating to TLS 1.2 after confirming there are no issues when TLS 1.0 and TLS 1.1 are disabled.